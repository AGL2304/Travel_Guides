{#
    Ce formulaire de suppression est conçu pour être flexible.
    Il accepte deux variables lors de l'inclusion :
    - button_label: Le texte à afficher sur le bouton (ex: 'Supprimer ce guide').
    - button_class: Les classes Tailwind CSS à appliquer au bouton.
#}
<form method="post" 
      action="{{ path('app_visite_delete', {'id': visite.id}) }}" 
      onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet élément ? Cette action est irréversible.');" 
      class="inline">
      
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ visite.id) }}">

    <button type="submit" class="{{ button_class|default('text-sm font-medium text-red-600 hover:text-red-800 transition-colors') }}">
        {{- button_label|default('Supprimer') -}}
    </button>
    
</form>